{
  "name": "appa-shop-root",
  "version": "1.0.0",
  "description": "Root configuration for Siva Auto Garage website deployment",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run install-all",
    "build": "npm run build --prefix frontend && rm -rf dist && mkdir -p dist && cp -r frontend/dist/* ./dist/",
    "start": "npm start --prefix backend",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
